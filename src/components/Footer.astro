---
import { useTranslations } from "../i18n/utils";
import { languageList } from "../i18n/ui";

const currentLang = Astro.currentLocale || "es";

const translateLabels = useTranslations(
    currentLang as keyof typeof languageList,
);
---

<footer class="m-4 dark:backdrop-blur-lg w-full xl:w-[1120px] mx-auto mb-10">
    <div class="w-full mx-auto max-w-screen-xl p-4 flex items-center justify-between">
        <span class="lg:text-sm sm:text-xs text-violet-800 dark:text-gray-400">© 2025 Inacora</span>
        <ul class="flex flex-wrap items-center lg:text-sm sm:text-xs font-medium text-gray-500 dark:text-white/90">
            <li>
                <a href="#about" class="hover:underline me-4 md:me-6">
                    {translateLabels("nav.about")}
                </a>
            </li>
            <li>
                <a href="#contact" class="hover:underline">
                    {translateLabels("nav.contact")}
                </a>
            </li>
        </ul>
    </div>
</footer>
